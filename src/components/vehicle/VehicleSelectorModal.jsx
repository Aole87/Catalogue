import React from 'react';

export const VehicleSelectorModal = ({ onSelectComplete }) => {
  // Disabled as per user request ("function ค้นหาตามภาพไม่ควรให้หลุดมาแสดง")
  return null;
};

export default VehicleSelectorModal;

